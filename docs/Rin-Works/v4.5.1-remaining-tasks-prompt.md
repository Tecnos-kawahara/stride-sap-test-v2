# Claude Code 指示プロンプト: Tecnos-STRIDE v4.5.1 残課題一括修正

> **作成日**: 2026-03-10（修正版 v4 — レビュー指摘 3回分を反映）
> **作成者**: Rin
> **対象**: Claude Code

---

作業ディレクトリ: /Users/j620h-okzk/ZINOKZ/sdd_template_enterprise

## タスク: Tecnos-STRIDE v4.5.0 — 残課題の一括修正

5件の修正。既存アーキテクチャとの整合性を厳守する。

**⚠️ 絶対に変更しないファイル:**
- `.planning/plan.md` — 別件のアクティブな作業計画。触らない。

---

### Task 1: README.md に PM クイックスタートを追加

`manual/00_pm_quickstart.md` は既に存在する。README.md に導線を追加する。

#### 1a. マニュアル一覧テーブルに追加

`README.md` の `## マニュアル` セクション内のテーブル（L549付近）の先頭行に追加:

```markdown
| # | 内容 |
|---|------|
| **00** | **⚡ PM クイックスタート（5分）** — 承認操作だけで開発を進める方法 |
| 01 | Getting Started |
```

#### 1b. 始め方セクションに NOTE 追加

`README.md` の `## 🚀 始め方` セクション内、最初の Step の直前に挿入:

```markdown
> 📖 **PM の方へ**: [PM クイックスタート（5分）](manual/00_pm_quickstart.md) を読めば、AI 自律開発の全体像と承認操作を5分で理解できます。
```

---

### Task 2: stride_lint.py に coverage_tier ミスマッチ WARN を追加

**実装場所**: `sdd-templates/tools/stride_lint.py`（通常版）
**理由**: `stride-lint` シェルスクリプト（L8）が `stride_lint.py` を呼ぶ。SYMPHONY.md の hook も通常版を使う。

**挿入箇所**: L1626 付近。既存の `bd = basic_doc.get("basic_design", {})` の直後、
`coverage_tier = bd.get("coverage_tier") or "standard"` の直後に追加:

```python
        coverage_tier = bd.get("coverage_tier") or "standard"

        # v4.5.1: Tier mismatch detection
        # Warn if existing flags suggest a different tier than what's declared
        if coverage_tier == "standard":
            _tier_hints = []
            if bd.get("security_sensitive"):
                _tier_hints.append("security_sensitive=true")
            if bd.get("erp_integration"):
                _tier_hints.append("erp_integration=true")
            if _tier_hints:
                result.add_warning(
                    "TIER_MISMATCH",
                    f"coverage_tier is 'standard' but {', '.join(_tier_hints)} "
                    f"suggests 'critical'. Consider updating coverage_tier."
                )
```

**参照するフィールドは `basic_design_template.md` に実在するもののみ**:
- `security_sensitive` (bool, L99)
- `erp_integration` (bool, L100)

---

### Task 3: Amendment Phase にファストパス追加

**現状の理解**:
- `cmd_analyze` (L369): Feature内のFindings/Decisions/影響WIを収集してレポート出力。独立コマンド。
- `cmd_draft` (L460): Markdown テンプレートを stdout に出す。
- `cmd_apply` (L785): PM承認チェック → Spec パッチ → PR作成。
- argparse の parser 変数名は `p_draft` (L1396)、`p_apply` (L1410付近)。

#### 3a. `p_draft` に `--fast` フラグ

`amendment_generator.py` L1396 付近の `p_draft` サブパーサーに追加:
```python
p_draft.add_argument("--fast", action="store_true",
    help="Low-risk fast track: skip analyze step, TECH_LEAD approval only")
```

`cmd_draft` 関数内で、`getattr(args, 'fast', False)` が True の場合:
1. `lines` リストの先頭（`f"## 📜 Amendment: {title}\n"` の前）に fast_track マーカーを挿入:
   ```
   > ⚡ **Fast Track Amendment** — analyze ステップ省略。TECH_LEAD 承認のみ。
   
   ```
2. 末尾の承認セクションを変更 — 通常の:
   ```markdown
   ### 承認
   - [ ] PM承認
   - [ ] Tech Lead承認（アーキテクチャ影響がある場合）
   ```
   を以下に置換:
   ```markdown
   ### 承認
   - [ ] Tech Lead承認
   ```

#### 3b. 承認チェックをヘルパー関数に切り出す

`cmd_apply` の承認チェックロジックを **テスト可能なヘルパー関数** として切り出す。
`cmd_apply` 関数の前（L785 より上）に追加:

```python
def check_amendment_approval(body: str) -> tuple[bool, str]:
    """Check if amendment body has required approval.
    
    Returns:
        (approved: bool, error_message: str)
        If approved is True, error_message is empty.
    """
    is_fast_track = bool(re.search(r'Fast Track Amendment', body))
    
    if is_fast_track:
        tl_approved = bool(re.search(r'- \[x\]\s*Tech Lead承認', body, re.IGNORECASE))
        if not tl_approved:
            return False, "Tech Lead承認が未完了です（Fast Track）。"
        return True, ""
    else:
        pm_approved = bool(re.search(r'- \[x\]\s*PM承認', body, re.IGNORECASE))
        if not pm_approved:
            return False, "PM承認が未完了です。"
        return True, ""
```

#### 3c. `cmd_apply` でヘルパーを使う

`cmd_apply` (L793付近) の既存の PM 承認チェックを以下に置換:

```python
    approved, err_msg = check_amendment_approval(body)
    if not approved:
        print(f"ERROR: {err_msg} Issue #{issue_num} のチェックボックスを確認してください。",
              file=sys.stderr)
        return 1
```

#### 3d. セルフテスト (`--test`) にテストケース追加

`amendment_generator.py` の `run_tests()` 関数（L1117付近）の末尾に以下のテストを追加:

```python
    # Test N: --fast flag produces fast track marker in draft
    print("Test N: Fast track draft output")
    import io
    from contextlib import redirect_stdout

    fast_args = argparse.Namespace(
        feature="FEAT-TEST-001", title="Fast Test", scope="",
        spec_sections="", findings="", decisions="", fast=True
    )
    buf = io.StringIO()
    with redirect_stdout(buf):
        cmd_draft(fast_args)
    fast_output = buf.getvalue()
    check("fast track marker present",
          "Fast Track Amendment" in fast_output,
          f"Expected 'Fast Track Amendment' in output")
    check("fast track has Tech Lead approval only",
          "- [ ] Tech Lead承認" in fast_output and "PM承認" not in fast_output,
          "Should have Tech Lead only, no PM")

    # Test N+1: Normal draft has PM approval
    normal_args = argparse.Namespace(
        feature="FEAT-TEST-001", title="Normal Test", scope="",
        spec_sections="", findings="", decisions="", fast=False
    )
    buf2 = io.StringIO()
    with redirect_stdout(buf2):
        cmd_draft(normal_args)
    normal_output = buf2.getvalue()
    check("normal draft has PM approval",
          "PM承認" in normal_output,
          "Normal draft should have PM承認")

    # Test N+2: check_amendment_approval() — fast track path
    print("Test N+2: check_amendment_approval (fast track)")
    ft_approved_body = "> ⚡ **Fast Track Amendment**\n- [x] Tech Lead承認\n"
    ft_unapproved_body = "> ⚡ **Fast Track Amendment**\n- [ ] Tech Lead承認\n"
    ok, msg = check_amendment_approval(ft_approved_body)
    check("fast track approved → True", ok, f"Expected True, got msg={msg}")
    ok2, msg2 = check_amendment_approval(ft_unapproved_body)
    check("fast track unapproved → False", not ok2, "Expected False for unapproved")
    check("fast track error mentions Tech Lead", "Tech Lead" in msg2, f"Got: {msg2}")

    # Test N+3: check_amendment_approval() — normal path
    print("Test N+3: check_amendment_approval (normal)")
    normal_approved_body = "### 承認\n- [x] PM承認\n- [ ] Tech Lead承認\n"
    normal_unapproved_body = "### 承認\n- [ ] PM承認\n"
    ok3, msg3 = check_amendment_approval(normal_approved_body)
    check("normal approved → True", ok3, f"Expected True, got msg={msg3}")
    ok4, msg4 = check_amendment_approval(normal_unapproved_body)
    check("normal unapproved → False", not ok4, "Expected False for unapproved")
    check("normal error mentions PM", "PM" in msg4, f"Got: {msg4}")
```

---

### Task 4: `symphony/__main__.py` の改善

修正後:
```python
"""Entry point for `python3 -m symphony`."""
import sys


def _main() -> None:
    try:
        from symphony.cli import main  # noqa: WPS433
    except ImportError as e:
        # Only catch missing *external* packages (jinja2, yaml, etc.)
        # Internal import errors (bugs in symphony/) propagate normally.
        if e.name and e.name.split(".")[0] not in ("symphony",):
            print(
                f"Error: Missing dependency '{e.name}' — {e}\n"
                "Run: pip install -r sdd-templates/requirements.txt",
                file=sys.stderr,
            )
            sys.exit(1)
        raise
    main()


if __name__ == "__main__":
    _main()
```

---

### Task 5: Tier ミスマッチ WARN の検証テスト

**重要**: `stride_lint.py` は `basic_design.md` 内の `# 0. Canonical Basic Design (YAML)` マーカー付き
fenced YAML ブロックを要求する（L1260）。さらに `spec.md`, `plan.md`, `tasks.md` も必要（L1213以降）。
frontmatter 形式や最小限の fixture では `MISSING_FILE` / `CANONICAL_BLOCK_NOT_FOUND` で落ちる。

**方法**: `sdd-templates/specs/sample_feature/` をコピーして改変する。

`sdd-templates/tools/test_tier_mismatch.py` として作成:

```python
#!/usr/bin/env python3
"""Verify TIER_MISMATCH warning in stride_lint.py.

Uses sdd-templates/specs/sample_feature/ as base fixture,
copies to temp dir, and modifies basic_design.md to test tier detection.
"""
import re
import shutil
import subprocess
import sys
import tempfile
from pathlib import Path

SCRIPT_DIR = Path(__file__).parent
PROJECT_ROOT = SCRIPT_DIR.parent.parent
SAMPLE = PROJECT_ROOT / "sdd-templates" / "specs" / "sample_feature"
LINT = SCRIPT_DIR / "stride_lint.py"


def patch_basic_design(bd_path: Path, sec: bool, erp: bool, tier: str = "standard") -> None:
    """Inject security_sensitive, erp_integration, coverage_tier into Canonical YAML."""
    text = bd_path.read_text()
    # Find the basic_design: block inside the Canonical YAML and inject fields
    # Insert after "basic_design:" line (inside the fenced yaml block)
    inject = f"""  coverage_tier: "{tier}"
  security_sensitive: {str(sec).lower()}
  erp_integration: {str(erp).lower()}"""

    # Insert after the first "basic_design:" inside ```yaml block
    pattern = r'(basic_design:\n)'
    replacement = r'\1' + inject + '\n'
    new_text = re.sub(pattern, replacement, text, count=1)
    if new_text == text:
        print(f"  WARNING: Could not inject fields into {bd_path}", file=sys.stderr)
    bd_path.write_text(new_text)


def run_lint_on_copy(sec: bool, erp: bool, tier: str, expect_warn: bool) -> bool:
    """Copy sample_feature, patch it, run lint, check for TIER_MISMATCH."""
    with tempfile.TemporaryDirectory() as td:
        feat = Path(td) / "specs" / "sample_feature"
        shutil.copytree(SAMPLE, feat)
        # Also copy memory/ for constitution.md reference
        mem_src = PROJECT_ROOT / "memory"
        mem_dst = Path(td) / "memory"
        if mem_src.exists():
            shutil.copytree(mem_src, mem_dst)

        patch_basic_design(feat / "basic_design.md", sec, erp, tier)

        r = subprocess.run(
            [sys.executable, str(LINT), str(feat)],
            capture_output=True, text=True, cwd=td
        )
        output = r.stdout + r.stderr
        found = "TIER_MISMATCH" in output
        ok = found == expect_warn
        status = "PASS" if ok else "FAIL"
        label = f"sec={sec} erp={erp} tier={tier} expect_warn={expect_warn}"
        print(f"  {status}: {label} (found={found})")
        if not ok:
            # Show lint output for debugging
            for line in output.splitlines():
                if "TIER" in line or "ERROR" in line or "WARN" in line:
                    print(f"    | {line}")
        return ok


def main():
    if not SAMPLE.exists():
        print(f"ERROR: Sample feature not found: {SAMPLE}", file=sys.stderr)
        sys.exit(1)

    print("TIER_MISMATCH warning tests:")
    print(f"  Sample: {SAMPLE}")
    print(f"  Lint:   {LINT}\n")

    results = [
        run_lint_on_copy(True, True, "standard", True),    # both flags → warn
        run_lint_on_copy(True, False, "standard", True),   # security only → warn
        run_lint_on_copy(False, True, "standard", True),   # erp only → warn
        run_lint_on_copy(False, False, "standard", False),  # neither → no warn
        run_lint_on_copy(True, True, "critical", False),   # already critical → no warn
    ]
    passed = sum(results)
    total = len(results)
    print(f"\n{passed}/{total} passed")
    sys.exit(0 if all(results) else 1)


if __name__ == "__main__":
    main()
```

---

### 最終検証

全修正完了後、以下を**すべて**実行:

```bash
source .venv/bin/activate

# 1. Symphony テスト — リグレッションなし確認
python3 -m pytest symphony/tests/ -v --tb=short
# 期待: 222+ passed, 0 failed

# 2. Tier ミスマッチ WARN の動作確認（sample_feature ベースの fixture）
python3 sdd-templates/tools/test_tier_mismatch.py
# 期待: 5/5 passed

# 3. amendment_generator セルフテスト — fast_track の draft + apply 検証を含めて全 PASS
python3 sdd-templates/tools/amendment_generator.py --test 2>&1 | tail -10
# 期待: 全テスト PASS（draft fast_track、apply fast_track detection 含む）

# 4. amendment_generator --fast の実出力確認
python3 sdd-templates/tools/amendment_generator.py draft \
  --feature FEAT-TEST-001 --title "テスト" --fast 2>&1 | head -3
# 期待: "⚡ **Fast Track Amendment**" が出力される

# 5. symphony -m 実行確認
python3 -m symphony --help 2>&1 | head -3
# 期待: ヘルプが表示される

# 6. 全 Symphony テスト再実行（最終確認）
python3 -m pytest symphony/tests/ -q
# 期待: 222+ passed
```

### 完了条件
- [ ] README.md に PM クイックスタートがリンク
- [ ] `stride_lint.py`（通常版）で TIER_MISMATCH WARN が動作
- [ ] `test_tier_mismatch.py` が 5/5 PASS（sample_feature ベース fixture）
- [ ] amendment_generator に `--fast` フラグ追加
- [ ] `check_amendment_approval()` ヘルパー関数がテスト可能な形で切り出されている
- [ ] セルフテストで **draft出力**、**`check_amendment_approval()` の fast track / normal 両パス** を検証
- [ ] `__main__.py` が外部依存のみキャッチ、内部バグは素通し
- [ ] Symphony テスト 222件+ PASS（リグレッションなし）
- [ ] `.planning/plan.md` は**変更していない**こと

完了したら:
```bash
git add -A && git commit -m "feat(v4.5.1): PM quickstart link, tier mismatch warn, amendment fast-track, improved entrypoint"
```
